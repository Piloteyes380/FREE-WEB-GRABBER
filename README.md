# FREE-WEB-GRABBER
A full-stack web app that lets users enter a URL to download a website’s source code. The backend fetches HTML and all linked CSS, JS, images, and fonts, then packages everything into a ZIP. Includes progress updates, error handling, and a clean modern UI.
